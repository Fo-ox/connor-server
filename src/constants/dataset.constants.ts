export enum DatasetColumnsKey {
    TYPE = 'type-',
    PRIORITY = 'priority-',
    ASSIGNEE = 'assignee-',
    ASSIGNEE_SKILL = 'assignee-skill-',
    INITIAL_ESTIMATE = 'initial-estimate-',
    TAGS = 'tag-',
    RESOLVED_ESTIMATE = 'resolved-estimate'
}

export const NORMALIZE_MIN = 1;
export const NORMALIZE_MAX = 200;
